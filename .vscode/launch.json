{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Julia Release",
      "type": "julia",
      "request": "launch",
      "program": "${workspaceFolder}/src/hello.jl",
      "args": ["--project=.", "-O3", "--compile=all"]
    }
  ]
}
